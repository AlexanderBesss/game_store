import { ParamIdValidationPipe } from './param-id-validation.pipe';

describe('ParamIdValidationPipe', () => {
  it('should be defined', () => {
    expect(new ParamIdValidationPipe()).toBeDefined();
  });
});
